{
    "name": "flywheel-task-manager",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm run build:backend && npm run build:copy && npm run build:api && npm run build:frontend",
        "build:backend": "cd backend && npm install && npm run build",
        "build:copy": "node -e \"require('fs').cpSync('backend/dist', 'api/backend', {recursive: true})\"",
        "build:api": "cd api && npm install",
        "build:frontend": "cd frontend && npm install && npm run build"
    },
    "workspaces": [
        "frontend",
        "backend",
        "api"
    ]
}